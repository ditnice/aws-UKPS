import { test } from '../fixtures/test'

test('changes a user role', async () => {
  test.fixme(true, 'The page is currently hardcoded and has no save action.')
})

test('deactivates and reactivates a user', async () => {
  test.fixme(true, 'The list exposes a user ID, but the API needs a membership ID.')
})

test('approves a registration request', async () => {
  test.fixme(true, 'The page has no action and no exposed backend endpoint.')
})

test('rejects a registration request', async () => {
  test.fixme(true, 'The page has no action and no exposed backend endpoint.')
})
