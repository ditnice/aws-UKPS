'use client'

import { Example } from '../../_components/Example'

export function Examples() {
  return (
    <Example title="React example">
      <p>
        The documented <code>&lt;InPageNav /&gt;</code> snippet is not standalone: it scans headings
        in the surrounding page and renders nothing when no matching headings exist. No additional
        example has been invented here.
      </p>
    </Example>
  )
}
